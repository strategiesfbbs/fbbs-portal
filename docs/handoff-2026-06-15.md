# FBBS Portal — Session Handoff (2026-06-15)

## TL;DR
Did a health review (all green) and **permanently fixed the recurring iCloud eviction-freeze** by moving the portal out of the iCloud-synced `~/Documents` tree to `~/fbbs-portal`. Cutover is complete and verified — the live portal now serves from the new location.

## Current state (verified this session)
- **Live portal:** `node server/server.js`, cwd **`/Users/donkey/fbbs-portal`**, serving http://localhost:3000.
- **Health:** `npm test` passes (exit 0); `GET /`, `/api/current`, `/api/offerings/all`, `/api/crm/dashboard`, `/api/market/wire`, bank search (12 rows) all 200. 142 MB `bank-data.sqlite` reads fine. **0 iCloud placeholders** under the new `data/`.
- **Git:** `main` @ `a06b591`, clean tree, in sync with `origin/main`.
- **Daily package:** 2026-06-12 (Fri), 10 required slots + companions, 248 offerings, published via folder-auto-publish. Normal.

## What changed
- Made a verified full copy `~/Documents/.../fbbs-portal-local-run-20260425` → `~/fbbs-portal` via `ditto` (8563/8563 files, fully materialized, 0 placeholders). `~/fbbs-portal` is **outside** the iCloud-synced Desktop/Documents tree, so eviction can't recur there.
- User did the cutover manually (the live shared service shouldn't be killed automatically): Ctrl+C old launcher → relaunched `~/fbbs-portal/start-portal.command`. Old Documents instance is stopped.
- Updated agent memory `icloud-eviction-freeze.md` with the new path + status.

## Root cause (for context)
`~/Documents` has iCloud "Desktop & Documents Folders" syncing **ON**, and disk was tight (~13 GB free of 228 GB). Under that pressure macOS Optimize Storage evicted file bodies to cloud placeholders (887 in `data/`, ~4800 tree-wide). The single-threaded server does sync reads (e.g. `cd-history/*.json`), and a read of an evicted file blocks until re-download — a stalled download froze the whole portal. Moving off the synced tree removes the eviction entirely.

## Open follow-ups (NOT yet done)
1. **Delete the old Documents copy** once confident (left as a safety net):
   ```
   rm -rf "/Users/donkey/Documents/Codex/2026-04-24/files-mentioned-by-the-user-fbbs/fbbs-portal-local-run-20260425"
   ```
2. **Re-make any Dock icon / Finder alias** to `~/fbbs-portal/start-portal.command` — the old one breaks after the delete.
3. **Don't run both installs at once** — they'd both bind :3000 and both watch the dropbox folder.

## For the next chat
**A new session may open with cwd = the OLD Documents path** (that's how sessions here were invoked). The real working copy is now **`/Users/donkey/fbbs-portal`** — operate there. If the old copy has been deleted, `cd /Users/donkey/fbbs-portal` first.

## Quick re-verify command
```
pid=$(lsof -ti tcp:3000); lsof -a -p $pid -d cwd 2>/dev/null | awk 'NR>1{print "serving from:",$NF}'
curl -s -o /dev/null -w "current=>%{http_code}\n" http://127.0.0.1:3000/api/current
```
