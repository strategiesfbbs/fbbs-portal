# FBBS Portal — Session Handoff (2026-06-25 morning)

Start-here for a fresh chat. Read this, then `CLAUDE.md` (== `AGENTS.md`) for the full architecture.

## Repo state
- Branch `main`, HEAD **`0f2201f`**, in sync with `origin/main`, working tree clean.
- `npm test` is green (47 suites). `npm run smoke:go-live` for a boot smoke (not in `npm test`).
- **Two agents share this one working tree** (Codex + Claude Code), both commit to `main`. This caused file-clobber races yesterday — prefer a git worktree for parallel work, commit small, push immediately. Note: the pre-commit `npm test` hook is **Claude-Code-only**, so Codex must run `npm test` by hand before committing.

## Active dual-agent split (authoritative: `docs/codex-overnight-audit-2026-06-25.md`)
- **Codex** — all backend/data correctness, route behavior, import semantics, tests, and the **Pershing vertical**.
- **Claude** — the **UI/CSS website sweep** (visual polish, layout, responsive, dead-CSS, design-system) + **product brainstorm**.
- Coordinate before crossing lanes. Codex-active files to avoid editing unprompted: `server/server.js`, `server/bank-signals.js`, `server/report-store.js`, `server/pershing-store.js`, `public/js/portal.js` (Pershing UI), `public/css/portal.css` (Pershing styles), `public/index.html`, `scripts/import-pershing-export.js`, `tests/pershing-store.test.js`.

## Shipped in the last ~24h (all on `main`)
- **Salesforce import** — `server/salesforce-import.js` (pure) + `scripts/import-salesforce-export.js` (CLI, dry-run default). 1,736 contacts imported into `bank_contacts`, idempotent via `salesforce_contact_id`; owner/status backfill applied (46 statuses, 1 owner); compliance columns added. Spec: `docs/salesforce-integration-spec-2026-06-24.md`. (`6dd91b5`, `a470e52`)
- **Security** — `/api/contacts/import` now admin-gated. (`ef897fc`)
- **Bond swap** — `validateLegsForSend` flags legs maturing ≤ settle date, wired into send. (`354a87c`)
- **Pershing account recency (Codex)** — account-level import/store/signal/report; `docs/current-state-2026-06-25.md`. (`a083aaa`)
- **Portal sweep audit** — 55 findings across 8 lanes: `docs/portal-sweep-audit-2026-06-25.md`. (`c4cce33`)
- **UI/CSS facelift (Claude)** — fixed 3 undefined CSS vars (`--green`×51 → racing-green, `--shadow-sm`, `--bg-soft`), global `:focus-visible`, `.small-btn.secondary`. (`0f2201f`)

## Today's open work

### Claude lane (UI/CSS + product) — recommended starting point
1. **Visual-QA the merged CSS fixes**, then work the facelift plan: `docs/ui-ux-facelift-plan-2026-06-25.md` (47 findings, phased — Phase 1 quick wins first). Preview via `.claude/launch.json` (`fbbs-portal` / `fbbs-portal-dev`).
2. **Design-system tokenization** (no build step, `:root` custom props): `docs/design-system-2026-06-25.md` — undefined-var cleanup is done; next is radius/spacing/type-scale consolidation + confidently-dead-CSS pruning (the named Coverage-Workspace classes are already gone — prune by grep-proven zero-ref blocks only).
3. **Product ideas**: `docs/portal-product-brainstorm-2026-06-25.md` (ranked; several already in `docs/feature-backlog-2026-06-24.md`).

### Codex lane (don't implement unprompted)
First batch from the overnight audit: Pershing wording rename (account-recency, not trade), the **Pershing trade-history import spec** (next data file needed — date/CUSIP/par/price), surface contact compliance badges, replace the legacy Upload-page contact import, stale tear-sheet state + contactId validation; then owner-scoping, account-universe consistency, and test gaps (Pershing HTTP, reports CRUD, CLI import scripts).

### Cross-cutting (need coordination — touch Codex files)
From the sweep's pending list: **report access-control** (reps can edit/delete each other's saved reports), **data-source divergence** (My Work reads `saved_banks`, Pulse reads `account_status`), opportunity default-assignee, non-admin `?rep=all` handling.

## Owner decisions pending
- RIAs (17.9k accounts + ~214 contacts) — no bank entity to attach to; deferred (would need a new RIA store).
- Retire the uploaded-HTML "Published Dashboard" slot (native Sales Dashboard supersedes it).
- Whether to replace the `account_status.owner` "A.W. Spellmeyer" catch-all (3,571 banks) with contact-derived owners.

## Guardrails
2 npm deps only (`pdf-parse`, `better-sqlite3`; SheetJS vendored) · no build step · SQLite only via `server/sqlite-db.js` (bound params) · strict CSP (vendored assets under `public/vendor`, no CDN) · never serve `_`-prefixed files · don't revive the retired Coverage Workspace · keep `CLAUDE.md` and `AGENTS.md` in sync.
